If the interpreter uses normal order evaluation (subsituting operands before evaluating), execution should work just fine. With applicative order, it will hang on trying to evaluate (p) repeatedly.

Turns out DrRacket uses applicative order.
